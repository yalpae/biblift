export default {
	myVar1: [],
	myVar2: {},
	var loc = navigator.geolocation;

	function long() {
		//	use async-await or promises
		
		if (loc) {
		}
		//	await storeValue('varName', 'hello world')
	}

	function lat() {
		//	use async-await or promises
		
		if (loc) {
			loc.
		}
		//	await storeValue('varName', 'hello world')
	}

}